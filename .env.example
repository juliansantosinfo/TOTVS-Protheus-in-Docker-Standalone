################################################################################
# TOTVS Protheus Standalone - Variáveis de Ambiente
################################################################################
# Este arquivo contém exemplos de configuração para diferentes cenários.
# Copie este arquivo para .env e ajuste os valores conforme necessário.
#
# IMPORTANTE: Nunca commite o arquivo .env com senhas reais no repositório!
################################################################################

################################################################################
# CONFIGURAÇÃO DO BANCO DE DADOS
################################################################################

# Tipo de banco de dados
# Valores: POSTGRES, MSSQL, ORACLE
DATABASE_TYPE=POSTGRES

# Endereço do servidor de banco de dados
# Exemplos: localhost, 192.168.1.100, db.example.com
DATABASE_SERVER=127.0.0.1

# Porta do banco de dados
# PostgreSQL: 5432 (padrão)
# MSSQL: 1433 (padrão)
# Oracle: 1521 (padrão)
DATABASE_PORT=5432

# Usuário do banco de dados
# PostgreSQL: postgres (padrão)
# MSSQL: sa (padrão)
# Oracle: system (padrão)
DATABASE_USERNAME=postgres

# Senha do banco de dados
# IMPORTANTE: Use senhas fortes em produção!
DATABASE_PASSWORD=postgres

# Nome do banco de dados
# Padrão: protheus
# Exemplos: protheus_dev, protheus_hml, protheus_prd
DATABASE_NAME=protheus

################################################################################
# MODO DE OPERAÇÃO
################################################################################

# Usar PostgreSQL embedded (dentro do container)
# 0 = Banco externo (requer DATABASE_SERVER configurado)
# 1 = Banco interno (ignora DATABASE_SERVER, usa localhost)
DATABASE_EMBEDDED=1

################################################################################
# RESTAURAÇÃO DE BACKUP
################################################################################

# Restaurar backup ao criar banco
# 0 = Não restaura (cria banco vazio)
# 1 = Restaura backup (se disponível)
DATABASE_RESTORE=1

# Tipo de backup a restaurar
# 0 = Backup base (estrutura básica, ~7MB)
# 1 = Backup completo (estrutura + dados, ~40MB)
DATABASE_RESTORE_FULL=0

################################################################################
# SERVIÇOS REST
################################################################################

# Habilitar REST no AppServer principal (porta 8080)
# 0 = Desabilitado
# 1 = Habilitado
# ATENÇÃO: Não pode ser usado junto com ENABLE_REST_SERVICE=1
ENABLE_REST_EMBEDDED=0

# Habilitar AppServer REST separado (portas 3234, 3235, 8080)
# 0 = Desabilitado
# 1 = Habilitado (inicia segundo AppServer dedicado para REST)
# ATENÇÃO: Não pode ser usado junto com ENABLE_REST_EMBEDDED=1
ENABLE_REST_SERVICE=0

################################################################################
# DEBUG E DESENVOLVIMENTO
################################################################################

# Modo debug do script de inicialização
# 0 = Desabilitado
# 1 = Habilitado (mostra comandos executados, pode expor senhas nos logs)
DEBUG_SCRIPT=0

################################################################################
# EXEMPLOS DE CONFIGURAÇÃO POR CENÁRIO
################################################################################

# ============================================================================
# CENÁRIO 1: Desenvolvimento Local (PostgreSQL Embedded)
# ============================================================================
# DATABASE_TYPE=POSTGRES
# DATABASE_EMBEDDED=1
# DATABASE_RESTORE=1
# DATABASE_RESTORE_FULL=1
# DEBUG_SCRIPT=0

# ============================================================================
# CENÁRIO 2: Desenvolvimento Local PostgreSQL Externo
# ============================================================================
# DATABASE_TYPE=POSTGRES
# DATABASE_SERVER=postgres-prod.internal
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=SenhaSegura123!
# DATABASE_NAME=protheus_prd
# DATABASE_EMBEDDED=0
# DATABASE_RESTORE=0

# ============================================================================
# CENÁRIO 3: Desenvolvimento Local MSSQL Externo
# ============================================================================
# DATABASE_TYPE=MSSQL
# DATABASE_SERVER=mssql-prod.internal
# DATABASE_PORT=1433
# DATABASE_USERNAME=sa
# DATABASE_PASSWORD=SenhaSegura123!
# DATABASE_NAME=protheus_prd
# DATABASE_EMBEDDED=0
# DATABASE_RESTORE=0

# ============================================================================
# CENÁRIO 4: Desenvolvimento Local Externo
# ============================================================================
# DATABASE_TYPE=ORACLE
# DATABASE_SERVER=oracle.example.com
# DATABASE_PORT=1521
# DATABASE_USERNAME=system
# DATABASE_PASSWORD=SenhaSegura123!
# DATABASE_NAME=PROTHEUSPRD
# DATABASE_EMBEDDED=0
# DATABASE_RESTORE=0

# ============================================================================
# CENÁRIO 5: Desenvolvimento com REST Separado
# ============================================================================
# DATABASE_TYPE=POSTGRES
# DATABASE_EMBEDDED=1
# DATABASE_RESTORE=1
# ENABLE_REST_SERVICE=1

# ============================================================================
# CENÁRIO 6: Desenvolvimento com REST Embedded
# ============================================================================
# DATABASE_TYPE=POSTGRES
# DATABASE_EMBEDDED=1
# DATABASE_RESTORE=1
# ENABLE_REST_EMBEDDED=1
